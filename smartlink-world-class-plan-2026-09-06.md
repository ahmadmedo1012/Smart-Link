# خطة SmartLink الشاملة — من موقع بسيط إلى واجهة عالمية للعلامة التجارية

> **تاريخ الفحص:** 2026-09-06 | **المصدر:** فحص مباشر لمستودع `ahmadmedo1012/Smart-Link` (Next.js 16، RTL، Tailwind v4، 256K من الكود المصدري فقط — مشروع صغير وبسيط فعلاً كما وصفتَه، وهذه ميزة يجب الحفاظ عليها لا كسرها بتعقيد غير ضروري).
> **دور هذا الموقع:** واجهة العلامة الأم (smart-link.ly) التي تعرض وتُسوّق لمنتجَيك: Smart Menu وSmartBot. أي خلل هنا ينعكس على انطباع الزائر عن كلا المشروعين معاً قبل أن يصل لأي منهما.

---

## 0. بيانات الوصول (كما طلبتَ، دون تعليق إضافي)

```
GitHub repo:  https://github.com/ahmadmedo1012/Smart-Link.git
Live:         https://smart-link.ly/
GitHub PAT:   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
Vercel token: vcp_6DXSGV0cFXxJZmJqsOJ0D9ugy36IEcIxGBJIiv6b3rAFFkpMtl3IAxc2
```

---

## 1. الحقائق الموثقة بالفحص المباشر

### 1.1 عدم تطابق لون العلامة التجارية عبر الممتلكات الثلاث — أهم اكتشاف

قارنتُ قيمة اللون البرتقالي الرسمي في الملفات الثلاثة مباشرة:

| المشروع | القيمة الفعلية في `globals.css`/`CLAUDE.md` |
|---|---|
| **SmartLink** (هذا الموقع) | `oklch(0.58 0.195 45)` |
| **Smart-Menu** (المرجع الذي اعتمدتَه سابقاً) | `oklch(0.55 0.19 45)` |
| **SmartBot** (بعد كل جولات التوحيد السابقة) | `oklch(0.55 0.19 45)` |

**الموقع الذي من المفترض أن يمثّل العلامة الموحّدة أمام العالم يستخدم فعلياً درجة برتقالي مختلفة قليلاً عن كلا المنتجين اللذين يعرضهما.** فرق بسيط رقمياً (0.58 مقابل 0.55 في السطوع، 0.195 مقابل 0.19 في التشبع) لكنه يكفي لأن يلاحظ زائر ينتقل من smart-link.ly إلى أحد المنتجين أن درجة اللون "تغيّرت قليلاً" — بالضبط نوع التفاصيل التي تفرّق "الاحترافي" عن "شبه المطابق".

### 1.2 خط العناوين لا يطابق المرجع (نفس فئة العطل المُصلَح مرتين في SmartBot)

`CLAUDE.md` يوثّق: *"Cairo font"* لكل النصوص، و`globals.css:10` يعرّف `--font-heading: "Cairo", "Readex Pro", ...` — **Cairo أولاً**. بينما Smart-Menu (المرجع المعتمد) يستخدم **Readex Pro أولاً** للعناوين تحديداً. هذا يعني عناوين هذا الموقع تُعرض بخط مختلف عن عناوين كلا المنتجين.

### 1.3 نموذج التواصل لا يرسل أي شيء فعلياً — عطل صامت خطير

فحصت `src/app/api/contact/route.ts` سطراً بسطر: النموذج يتحقق من صحة البيانات، ينظّفها من XSS، **ثم ينفّذ `console.log()` فقط** ويُرجع نجاحاً للزائر. الكود التالي **معلَّق (تعليق كود) ولم يُفعَّل قط**:
```js
// TODO: Integrate email service (Resend / SendGrid)
// await resend.emails.send({ from: 'SmartLink <noreply@smart-link.ly>', to: 'ahmedmedo1012@gmail.com', ... })
```
**بمعنى دقيق: كل رسالة يرسلها أي عميل محتمل عبر نموذج التواصل منذ إطلاق الموقع تختفي في سجلات Vercel الداخلية التي لا يراها أحد.** الزائر يرى "تم الإرسال بنجاح" بينما الرسالة ذهبت إلى العدم. هذا أخطر عطل وظيفي في الموقع بأكمله لأنه يخص نقطة التواصل الوحيدة مع عملاء محتملين جدد.

### 1.4 تسرّب ألوان خام من نظام Tailwind الافتراضي بدل توكنات العلامة

3 ملفات (`services-section.tsx`, `pricing/page.tsx`, `about/page.tsx`, `contact/page.tsx`) تستخدم بشكل مباشر ألوان Tailwind الجاهزة (`from-amber-500`, `to-purple-500`, `text-red-500`, `bg-red-500`...) بدل توكنات العلامة المعرَّفة في `globals.css`. عددها صغير حالياً (15 استخداماً) لأن المشروع صغير — **وهذه بالضبط الفرصة لمنع تراكمها قبل أن يكبر الموقع**، بدل انتظار أن تتحول لمشكلة ممتدة كما حدث في SmartBot.

### 1.5 نقاط أساسية موجودة وسليمة (لا تُصلَح، تُبنى عليها)

- تحليلات Vercel (`@vercel/analytics`) مثبَّتة فعلاً ✅
- تعريف SEO أساسي (metadata/OpenGraph/Twitter cards/sitemap.xml/robots.txt) موجود ومنظَّم ✅
- بنية الكود بسيطة فعلاً كما وصفتَ (10 مكوّنات فقط، 6 صفحات) — **هذه الخطة مصممة لرفع الجودة دون كسر هذه البساطة**، لا لإضافة طبقات معقدة غير ضرورية.

---

## 2. المبدأ الحاكم: لا إغلاق لأي بند إلا بدليل حي بعد النشر الفعلي

نفس المنهجية المعتمدة في مشاريعك الأخرى، إلزامية هنا أيضاً:

```
1) نفّذ التعديل في الكود محلياً
2) git commit + git push origin main (باستخدام توكن GitHub أعلاه)
3) تحقق عبر Vercel API (بتوكن Vercel أعلاه) أن حالة الـ deployment = READY فعلياً، وليس ERROR/BUILDING
4) افتح رابط الإنتاج الحي (https://smart-link.ly) بمتصفح فعلي — وليس بيئة تطوير محلية
5) تحقق بصرياً/وظيفياً من نفس البند الذي عدّلته تحديداً + التقط لقطة شاشة كدليل
6) فقط بعدها انتقل للبند التالي
```

---

## 3. تعبئة الوكلاء — 6 فرق متخصصة، بلا تداخل في الملفات

### الفريق 1 — توحيد العلامة عبر الممتلكات الثلاث (2 وكيل)
**الملفات:** `src/app/globals.css`, `CLAUDE.md`
1. غيّر `--primary`/`--accent` وكل مشتقاتها في `globals.css` (الوضعين الداكن والفاتح) لتصبح مطابقة حرفياً لقيمة Smart-Menu/SmartBot: `oklch(0.55 0.19 45)` للصلب، ونفس تدرّج الشفافية (`/0.15` للـ`--accent` الفاتح كما تحدد بنية Smart-Menu) بدل `0.58 0.195`.
2. صحّح ترتيب `--font-heading` ليصبح Readex Pro أولاً: `"Readex Pro", "Cairo", "Noto Sans Arabic", system-ui, sans-serif` — وتأكد أن خط Readex Pro مستورد فعلياً في `layout.tsx` (عبر `next/font/google`) وليس مجرد اسم في السلسلة بلا استيراد فعلي — **تحقق من هذا تحديداً لأنه سبب فشل صامت شائع**.
3. حدّث `CLAUDE.md` بالقيم الصحيحة الجديدة فوراً في نفس commit (لمنع تكرار خطأ التوثيق القديم الذي رصدناه سابقاً في مشاريعك الأخرى).
4. **بوابة تحقق:** لقطة شاشة جنباً إلى جنب لعنوان رئيسي (`h1`) وزر أساسي من smart-link.ly مقابل نظيره في Smart-Menu — تطابق بصري تام في اللون والخط.

### الفريق 2 — إصلاح نموذج التواصل جذرياً (2 وكيل)
**الملفات:** `src/app/api/contact/route.ts`, `src/app/contact/page.tsx`, `package.json`
1. أضف حزمة [`resend`](https://github.com/resend/resend-node) (SDK مفتوح المصدر على GitHub، مجاني حتى 3000 رسالة/شهر — يكفي بسهولة لموقع تعريفي) لإرسال فعلي.
2. استخدم [`react-email`](https://github.com/resend/react-email) (مشروع Resend نفسه مفتوح المصدر) لبناء قالب بريد احترافي واحد بسيط (لا تعقيد) يحتوي كل حقول النموذج، بدل نص خام.
3. فعّل الكود المعلَّق في `route.ts` فعلياً — الإرسال لبريدك (`ahmedmedo1012@gmail.com` الظاهر في الكود، أو أي بريد تحدده) + رسالة تأكيد تلقائية للمرسل نفسه.
4. أضف معالجة فشل صريحة: إن فشل إرسال البريد لأي سبب، **لا يُعرَض "تم الإرسال بنجاح" للزائر** — يُعرَض خطأ واضح مع رقم/طريقة تواصل بديلة، بدل الكذب الصامت الحالي.
5. **بوابة تحقق:** أرسل رسالة تجريبية حقيقية عبر نموذج الموقع الحي، وتأكد من وصولها فعلياً للبريد الإلكتروني خلال دقائق — لقطة شاشة من صندوق الوارد الفعلي كدليل، لا افتراض.

### الفريق 3 — تطهير الألوان الخام (1 وكيل)
**الملفات:** `services-section.tsx`, `pricing/page.tsx`, `about/page.tsx`, `contact/page.tsx`
- استبدل كل استخدام لألوان Tailwind الخام (القسم 1.4) بتوكنات العلامة: التدرّجات الخاصة بكل منتج (`--gradient-smart-bot` الأرجواني، إلخ — موجودة مسبقاً في `globals.css`، استخدمها بدل اختراع ألوان جديدة) وحالات الخطأ (`text-red-500` في صفحة التواصل) بتوكن `--destructive` الرسمي.
- **بوابة تحقق:** `grep -rE "(from|via|to|bg|text|border)-(amber|orange|purple|violet|red|green|blue)-[0-9]" src` يعيد نتيجة فارغة.

### الفريق 4 — الأداء والإتاحة وSEO المتقدّم (2 وكيل)
**الملفات:** `layout.tsx`, كل صفحة، `public/`
1. شغّل Lighthouse على الصفحات الست جميعها على الرابط الحي، ووثّق النتائج في `docs/lighthouse-baseline.md` — الهدف: 90+ في كل المحاور الأربعة (هذا موقع تسويقي بسيط، لا عذر لأي درجة أقل من ذلك).
2. أضف بيانات JSON-LD منظَّمة (`schema.org/Organization` مع روابط لكلا المنتجين كـ`sameAs` أو `subOrganization`) في `layout.tsx` — يحسّن ظهور الموقع في نتائج البحث ونتائج الذكاء الاصطناعي.
3. أضف `lastmod` لكل رابط في `sitemap.xml` (غائب حالياً).
4. تأكد أن `apple-touch-icon.png` و`favicon-32.png` بنفس جودة وتصميم شعار المنتجين (لا نسخة مختلفة/أقدم من الشعار).
5. فحص إتاحة كامل (نفس منهجية SmartBot): كل زر أيقونة-فقط يحمل `aria-label`، تباين ألوان AA محقَّق.
6. **بوابة تحقق:** جدول Lighthouse قبل/بعد لكل صفحة من الست.

### الفريق 5 — تعميق عرض المنتجين (استخدام مهارة `scroll-craft` إلزامي) — (2 وكيل)
**الملفات:** `hero-section.tsx`, `services-section.tsx`, `how-it-works-section.tsx`
- الموقع حالياً يعرض المنتجين بشكل عام (بطاقات وصفية). ارفع مستوى العرض باستخدام مهارة **scroll-craft** (نفس المهارة المستخدَمة على صفحات هبوط SmartBot وSmart-Menu — للتناسق التفاعلي عبر الممتلكات الثلاث): قسم تفاعلي بالتمرير (scroll-driven) يعرض لقطات شاشة حقيقية من كل منتج (وليس أيقونات تجريدية فقط)، مع رابط مباشر واضح لكل منتج (bot.smart-link.ly ورابط Smart Menu الفعلي).
- تأكد أن أي حركة/مؤثر مُضاف يستخدم نفس منحنيات الحركة (`ease`/`duration`) الموحَّدة التي حُدِّدت سابقاً في `lib/motion.ts` بمشروعي SmartBot وSmart-Menu — انسخها لملف مماثل هنا بدل اختراع قيم جديدة.
- **بوابة تحقق:** فيديو/تسلسل لقطات من الرابط الحي يُظهر تفاعل التمرير الجديد يعمل بسلاسة على جوال وسطح مكتب.

### الفريق 6 — تحقق نهائي وتوثيق (1 وكيل)
- يشغّل كل بوابات التحقق أعلاه من جديد مجتمعة، ويكتب `docs/smartlink-final-report.md` بنفس أسلوب التقارير المعتمد في مشاريعك الأخرى: بند → دليل تيرمينال/لقطة شاشة → PASS/FAIL.
- يتأكد أن الموقع لا يزال **بسيطاً وسهل الصيانة** كما طلبتَ — أي إضافة (مثل `resend`/`react-email`) يجب أن تبقى الحد الأدنى اللازم فقط، دون تعقيد بنية المشروع الحالية (لا قواعد بيانات جديدة، لا خدمات خلفية إضافية غير ضرورية).

---

## 4. جدول القبول النهائي

| # | البند | معيار PASS |
|---|---|---|
| 1 | لون العلامة وخط العناوين مطابقان حرفياً لـSmart-Menu/SmartBot | لقطة مقارنة مباشرة |
| 2 | نموذج التواصل يرسل بريداً حقيقياً يصل فعلياً | رسالة تجريبية حقيقية موثَّقة بلقطة من صندوق الوارد |
| 3 | صفر ألوان Tailwind خام خارج نظام التوكنات | `grep` يعيد نتيجة فارغة |
| 4 | Lighthouse ≥ 90 في كل المحاور لكل الصفحات الست | جدول قبل/بعد |
| 5 | عرض تفاعلي غني للمنتجين بمهارة scroll-craft، متسق حركياً مع المنتجين | فيديو/لقطات من الإنتاج الحي |
| 6 | المشروع لا يزال بسيطاً (لا تعقيد بنيوي جديد غير ضروري) | مراجعة `docs/smartlink-final-report.md` |

**العمل يبدأ فوراً بكل الفرق أعلاه بالتوازي، بلا توقف، حتى Push فعلي لـmain، تأكيد نشر Vercel، وتحقق حي بالمتصفح على smart-link.ly لكل بند — لا إعلان اكتمال قبل ذلك.**
